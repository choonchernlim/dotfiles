{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "command -v jq >/dev/null || exit 0; f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.nix) command -v nixfmt >/dev/null && nixfmt \"$f\";; esac; exit 0"
          }
        ]
      }
    ]
  }
}
